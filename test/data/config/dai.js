// This config file is only provided to test deploys locally

const config = {
  name: 'DAI Token',
  symbol: 'DAI',
  decimals: 18
}

module.exports = {
  rpc: config,
  rinkeby: config,
  ropsten: config
}
