// This config file is only provided to test deploys locally

const config = {
  name: 'Aragon Network Token',
  symbol: 'ANT',
  decimals: 18
}

module.exports = {
  rpc: config,
  staging: config,
  ropsten: config,
  rinkeby: config,
  usability: config,
}
